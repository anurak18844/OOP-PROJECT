            Setup Project
            
----------set virtual environment------------
1. pip install --upgrade pip
2. pip install virtualenv
3. virtualenv environment_name
----------------------------------------------

--install package in requiremets in a project--
1. pip install -r requirements.txt
-----------------------------------------------